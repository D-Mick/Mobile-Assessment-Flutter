import 'package:flutter/material.dart';


class Shipify extends StatelessWidget {
  const Shipify({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
